def count(message):
    letter_counts = {}
    for letter in message:
        letter_counts[letter] = letter_counts.get(letter, 0) + 1
    print(letter_counts)
message = input("Enter a message : ")
count(message)